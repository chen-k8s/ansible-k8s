---
- name: 安装容器运行时 - 所有节点
  hosts: k8s_cluster
  roles:
    - containerd

- name: 安装 Kubernetes 组件 - 所有节点
  hosts: k8s_cluster
  roles:
    - kubernetes

- name: 部署 HAProxy + Keepalived - 仅 Master 节点
  hosts: master
  roles:
    - haproxy_keepalived
